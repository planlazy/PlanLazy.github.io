{
  "name": "PlanLazy.com",
  "version": "0.2.1",
  "dependencies": {
    "harp": "^0.19.0",
    "sitemap-generator-cli": "^3.1.2"
  },
  "scripts": {
    "build": "harp compile _public ./",
    "lint": "gulp lint",
    "sitemap": "sitemap-generator --query http://planlazy.com --path _public",
    "prestart": "npm run build",
    "start": "harp server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/planlazy/PlanLazy.github.io.git"
  },
  "author": "Dan BROOKS",
  "license": "UNLICENSED",
  "private": true
}
